plugin: amazon.aws.aws_ec2
regions: 
  - ap-southeast-2

# filters:
#   # All instances with their `Environment` tag set to `dev`
#   tag:Environment: WebServer

# # Ignores 403 errors rather than failing
# strict_permissions: False
